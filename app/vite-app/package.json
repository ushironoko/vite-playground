{
  "name": "vite-playground",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --jsx-factory=h",
    "build": "vite build",
    "DEBUG-dev": "DEBUG=vite* vite",
    "DEBUG-build": "DEBUG=vite* vite build",
    "lint": "eslint --ext .ts,.vue .",
    "fix": "eslint --fix --ext .ts,.vue ."
  },
  "dependencies": {
    "@vite-playground/base": "*",
    "vue": "^3.0.0-beta.7"
  }
}
